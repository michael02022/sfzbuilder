// This doesn't work from here, copy to your keybindings.json instead
[
	{
		"key":     "ctrl+super+d",
		"command": "workbench.action.tasks.runTask",
		"args":    "Qt Designer"
	},
	{
		"key":     "ctrl+super+u",
		"command": "workbench.action.tasks.runTask",
		"args":    "Save UI"
	}
]
